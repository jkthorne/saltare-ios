# Swift Package Manager
.build/
.swiftpm/
Package.resolved

# Xcode — the project is generated from project.yml by XcodeGen; never commit it
*.xcodeproj
*.xcworkspace
# Info.plist is generated by XcodeGen from project.yml's info.properties
Saltare/Info.plist
xcuserdata/
DerivedData/
*.xcuserstate
*.xcscmblueprint

# macOS
.DS_Store

# Secrets — never commit API keys, signing material, or credentials
*.p8
*.p12
*.mobileprovision
secrets.xcconfig
